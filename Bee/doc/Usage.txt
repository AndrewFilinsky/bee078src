
The Bee 0.7.8 build 0148 archiver (c) 1999-2005 Andrew Filinsky, Gomel city
(Belorussia), Melchiorre Caruso, Salemi city (Italia), 
Freeware version, Jun 2005.

  Usage: Bee <Command> -<Option 1> -<Option N> <ArchiveName> <FileNames...>

  Commands:

    a   Add files to archive
    d   Delete files from archive
    e   Extract files from archive
    x   eXtract files from archive with path name
    l   List archive
    t   Test archive files
    r   Rename files in archive

  Options:

    r       Recurse subdirectories
    u       Update files to archive
    f       Freshen files to archive
    e       force file Extention
    s       create Solid archive
    m<0..3> set compression Method (0-store...1-default...3-maximal)
    d<1..9> set Dictionary size (d1 uses < 5M, d2 (default) < 10M, d3 < 20M...)

    x       eXclude filenames
    t       Test files after adding
    c<dir>  insert (with 'a' cmd) or delete (with 'x' cmd) part of file path
    pri<P>  set process Priority (0-Idle, 1-Normal, 2-High, 3-RealTime)

  Use BeeOpt to make most optimal parameters.

  Type "Bee | more" if You can not see whole usage.
